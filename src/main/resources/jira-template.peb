{
    "fields": {
        "project": {
            "key": "{{ projectKey }}"
        },
        "summary": "{{ summary }}",
        "description": "{{ description }}",
        "issuetype": {
            "name": "Story"
        },
        "labels": [
            "kestra-bot"
        {% for label in labels %}
            , "{{ label }}"
        {% endfor %}
        ]
    }
}